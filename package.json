{
  "name": "chaii",
  "packageManager": "bun@1.1.7",
  "private": true,
  "license": "GPL-3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "prettier --write \"**/*.ts{x,}\"",
    "clean": "rm -rf node_modules .turbo",
    "add-component": "bun --filter=@chaii/ui run add-component",
    "ui:add": "bun --cwc=packages/ui add-safe",
    "es:add": "bun --cwc=packages/config-eslint add-safe",
    "ts:add": "bun --cwc=packages/config-typescript add-safe",
    "tw:add": "bun --cwc=packages/config-tailwind add-safe",
    "web:add": "bun --cwd=apps/web add-safe"
  },
  "dependencies": {
    "@types/bun": "^1.1.2",
    "@zemble/utils": "^0.7.34",
    "turbo": "latest"
  }
}
